<Window x:Class="SummonMe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SummonMe"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize"
        Title="MainWindow" 
        Height="600" 
        Width="900">

    <Grid>
        <StackPanel Margin="100" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBox Margin="50 0" Text="{Binding SummonerName}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Summoner"/>
            <TextBox Margin="50 0" Text="{Binding Region}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Region"/>
            <Button x:Name="Button" Margin="25 0 25 300" Content="SHOW DATA" Click="Button_Click"/>
        </StackPanel>
        <StackPanel Margin="200" Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel Margin="20" Orientation="Vertical" HorizontalAlignment="Center">
                <TextBlock Margin="20 15" Text="Wins" FontSize="15" Foreground="LightGray"/>
                <TextBlock Margin="20 15" FontSize="30" Text="{Binding Path=LeagueEntry.Wins}"/>
            </StackPanel>
            <StackPanel Margin="20" Orientation="Vertical" HorizontalAlignment="Center">
                <TextBlock Margin="20 15" Text="Loses" FontSize="15" Foreground="LightGray"/>
                <TextBlock Margin="20 15" FontSize="30" Text="{Binding Path=LeagueEntry.Losses}"/>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Window>
